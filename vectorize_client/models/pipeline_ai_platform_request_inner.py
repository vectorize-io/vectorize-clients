# coding: utf-8

"""
    Vectorize API (Beta)

    API for Vectorize services

    The version of the OpenAPI document: 0.0.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from vectorize_client.models.bedrock2 import Bedrock2
from vectorize_client.models.openai2 import Openai2
from vectorize_client.models.vertex2 import Vertex2
from vectorize_client.models.voyage2 import Voyage2
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

PIPELINEAIPLATFORMREQUESTINNER_ONE_OF_SCHEMAS = ["Bedrock2", "Openai2", "Vertex2", "Voyage2"]

class PipelineAIPlatformRequestInner(BaseModel):
    """
    PipelineAIPlatformRequestInner
    """
    # data type: Bedrock2
    oneof_schema_1_validator: Optional[Bedrock2] = None
    # data type: Vertex2
    oneof_schema_2_validator: Optional[Vertex2] = None
    # data type: Openai2
    oneof_schema_3_validator: Optional[Openai2] = None
    # data type: Voyage2
    oneof_schema_4_validator: Optional[Voyage2] = None
    actual_instance: Optional[Union[Bedrock2, Openai2, Vertex2, Voyage2]] = None
    one_of_schemas: Set[str] = { "Bedrock2", "Openai2", "Vertex2", "Voyage2" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PipelineAIPlatformRequestInner.model_construct()
        error_messages = []
        match = 0
        # validate data type: Bedrock2
        if not isinstance(v, Bedrock2):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Bedrock2`")
        else:
            match += 1
        # validate data type: Vertex2
        if not isinstance(v, Vertex2):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Vertex2`")
        else:
            match += 1
        # validate data type: Openai2
        if not isinstance(v, Openai2):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Openai2`")
        else:
            match += 1
        # validate data type: Voyage2
        if not isinstance(v, Voyage2):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Voyage2`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PipelineAIPlatformRequestInner with oneOf schemas: Bedrock2, Openai2, Vertex2, Voyage2. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PipelineAIPlatformRequestInner with oneOf schemas: Bedrock2, Openai2, Vertex2, Voyage2. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into Bedrock2
        try:
            instance.actual_instance = Bedrock2.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Vertex2
        try:
            instance.actual_instance = Vertex2.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Openai2
        try:
            instance.actual_instance = Openai2.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Voyage2
        try:
            instance.actual_instance = Voyage2.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PipelineAIPlatformRequestInner with oneOf schemas: Bedrock2, Openai2, Vertex2, Voyage2. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PipelineAIPlatformRequestInner with oneOf schemas: Bedrock2, Openai2, Vertex2, Voyage2. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], Bedrock2, Openai2, Vertex2, Voyage2]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


